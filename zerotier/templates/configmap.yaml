apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "zerotier.fullname" . }}
  labels:
    {{- include "zerotier.labels" . | nindent 4 }}
data:
  # Placeholder for future configuration
  README: |
    ZeroTier Configuration
    Network ID: {{ .Values.zerotier.networkId | default "not-set" }}

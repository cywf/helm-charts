apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "zerotier.serviceAccountName" . }}
  labels:
    {{- include "zerotier.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
